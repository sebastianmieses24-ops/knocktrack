<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
<title>Knock Track — by Green Nova</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=Space+Mono:wght@400;700&family=Rajdhani:wght@500;600;700&display=swap" rel="stylesheet">
<style>
:root {
  --bg: #0f1117;
  --surface: #1a1d27;
  --surface2: #22263a;
  --border: #2e3347;
  --text: #e8eaf0;
  --muted: #7a8099;
  --accent: #4f8ef7;
  --green: #4ade80;
  --green-dim: #22c55e;
}

* { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }

html { height:100%; }

body {
  font-family:'DM Sans',sans-serif;
  background:var(--bg);
  color:var(--text);
  height:100%;
  overflow:hidden;
}

/* ═══════════════════════════════
   DASHBOARD
═══════════════════════════════ */
#dashboard {
  position:fixed;
  inset:0;
  background:var(--bg);
  z-index:3000;
  display:flex;
  flex-direction:column;
  overflow-y:auto;
}

#dashboard-topbar {
  display:flex;
  align-items:center;
  justify-content:space-between;
  padding:16px 24px;
  background:var(--surface);
  border-bottom:1px solid var(--border);
  flex-shrink:0;
}

.gn-logo-title {
  font-family:'Rajdhani',sans-serif;
  font-size:26px;
  font-weight:700;
  letter-spacing:4px;
  line-height:1;
}
.gn-logo-title .green { color:var(--green); }
.gn-logo-title .white { color:var(--text); }
.gn-logo-line { width:100%; height:1px; background:linear-gradient(90deg,var(--green),#c9a84c); margin-top:3px; }
.gn-logo-sub { font-family:'Rajdhani',sans-serif; font-size:10px; font-weight:500; letter-spacing:4px; color:var(--muted); text-transform:uppercase; }

#open-map-btn {
  padding:10px 20px;
  background:var(--green-dim);
  color:#000;
  border:none;
  border-radius:9px;
  font-family:'DM Sans',sans-serif;
  font-size:14px;
  font-weight:700;
  cursor:pointer;
  white-space:nowrap;
}

#dashboard-content { padding:20px; flex:1; }

.dash-section-title {
  font-family:'Rajdhani',sans-serif;
  font-size:12px;
  font-weight:700;
  letter-spacing:3px;
  text-transform:uppercase;
  color:var(--muted);
  margin-bottom:12px;
  display:flex;
  align-items:center;
  gap:8px;
  cursor:pointer;
  user-select:none;
}
.dash-section-title::after { content:''; flex:1; height:1px; background:var(--border); }
.section-dot { width:8px; height:8px; border-radius:50%; flex-shrink:0; }
.collapse-arrow { font-size:11px; color:var(--muted); margin-left:auto; transition:transform 0.25s; flex-shrink:0; }
.collapse-arrow.rotated { transform:rotate(-90deg); }
.collapsible-body { overflow:hidden; max-height:2000px; transition:max-height 0.35s ease, opacity 0.25s; opacity:1; }
.collapsible-body.collapsed { max-height:0; opacity:0; }

.stats-grid {
  display:grid;
  grid-template-columns:repeat(auto-fill,minmax(150px,1fr));
  gap:10px;
  margin-bottom:24px;
}
.stat-card {
  background:var(--surface);
  border:1px solid var(--border);
  border-top:3px solid transparent;
  border-radius:12px;
  padding:14px;
  display:flex;
  flex-direction:column;
  gap:5px;
}
.stat-card-label { font-size:11px; font-weight:700; text-transform:uppercase; letter-spacing:0.8px; color:var(--muted); display:flex; align-items:center; gap:5px; }
.stat-card-icon { width:18px; height:18px; border-radius:50%; display:flex; align-items:center; justify-content:center; font-size:10px; flex-shrink:0; }
.stat-card-value { font-family:'Space Mono',monospace; font-size:28px; font-weight:700; line-height:1; }
.stat-card-sub { font-size:11px; color:var(--muted); }

.disposition-table { background:var(--surface); border:1px solid var(--border); border-radius:12px; overflow:hidden; margin-bottom:24px; }
.disposition-row { display:flex; align-items:center; padding:10px 14px; border-bottom:1px solid var(--border); gap:10px; }
.disposition-row:last-child { border-bottom:none; }
.disp-pin { width:24px; height:24px; border-radius:50%; display:flex; align-items:center; justify-content:center; font-size:12px; flex-shrink:0; font-weight:900; color:#fff; }
.disp-label { flex:1; font-size:13px; font-weight:600; }
.disp-count { font-family:'Space Mono',monospace; font-size:15px; font-weight:700; min-width:32px; text-align:right; }
.disp-bar-wrap { width:80px; height:5px; background:var(--border); border-radius:3px; overflow:hidden; }
.disp-bar { height:100%; border-radius:3px; }
.dash-two-col { display:grid; grid-template-columns:1fr 1fr; gap:20px; }

.time-tab { padding:6px 10px; border:none; border-radius:6px; background:transparent; font-family:'DM Sans',sans-serif; font-size:12px; font-weight:600; color:var(--muted); cursor:pointer; white-space:nowrap; }
.time-tab.active-time { background:var(--accent); color:#fff; }

/* ═══════════════════════════════
   MAP SHELL — the key fix
   Use absolute positioning so map
   always fills exactly the right area
═══════════════════════════════ */
#map-shell {
  position:fixed;
  top:0; left:0; right:0; bottom:0;
  display:flex;
  flex-direction:column;
}

#topbar {
  position:relative;
  z-index:100;
  display:flex;
  align-items:center;
  gap:8px;
  padding:8px 12px;
  background:var(--surface);
  border-bottom:1px solid var(--border);
  flex-shrink:0;
}

#topbar-logo {
  display:flex;
  align-items:center;
  gap:8px;
  flex-shrink:0;
}

#search-wrap {
  flex:1;
  position:relative;
  min-width:0;
}

#address-search {
  width:100%;
  padding:9px 12px 9px 36px;
  background:var(--surface2);
  border:1px solid var(--border);
  border-radius:8px;
  color:var(--text);
  font-family:'DM Sans',sans-serif;
  font-size:14px;
  outline:none;
  -webkit-appearance:none;
}
#address-search:focus { border-color:var(--accent); }
#address-search::placeholder { color:var(--muted); }

.search-icon { position:absolute; left:11px; top:50%; transform:translateY(-50%); color:var(--muted); font-size:14px; pointer-events:none; }

#stats-bar { display:flex; gap:8px; align-items:center; flex-shrink:0; }
.stat-pill { display:flex; align-items:center; gap:4px; padding:5px 9px; border-radius:20px; font-size:12px; font-weight:600; background:var(--surface2); border:1px solid var(--border); white-space:nowrap; }
.stat-dot { width:7px; height:7px; border-radius:50%; }

#map-and-sidebar {
  flex:1;
  display:flex;
  flex-direction:row;
  overflow:hidden;
  min-height:0;
}

#map-container {
  flex:1;
  position:relative;
  min-width:0;
  min-height:0;
}

#google-map {
  position:absolute;
  inset:0;
  width:100%;
  height:100%;
}

#map-hint {
  position:absolute;
  bottom:24px;
  left:50%;
  transform:translateX(-50%);
  background:rgba(26,29,39,0.9);
  border:1px solid var(--border);
  border-radius:12px;
  padding:9px 16px;
  font-size:13px;
  color:var(--muted);
  z-index:10;
  backdrop-filter:blur(8px);
  pointer-events:none;
  white-space:nowrap;
}

#map-type-toggle {
  position:absolute;
  top:12px;
  right:12px;
  z-index:10;
  display:flex;
  gap:5px;
}
.map-type-btn {
  padding:7px 12px;
  border-radius:8px;
  border:2px solid transparent;
  font-family:'DM Sans',sans-serif;
  font-size:12px;
  font-weight:700;
  cursor:pointer;
  backdrop-filter:blur(8px);
}
.map-type-btn.inactive { background:rgba(26,29,39,0.85); color:#aaa; border-color:rgba(255,255,255,0.1); }
.map-type-btn.active-type { background:var(--accent); color:#fff; border-color:var(--accent); }

/* sidebar */
#sidebar {
  width:300px;
  background:var(--surface);
  border-left:1px solid var(--border);
  display:flex;
  flex-direction:column;
  overflow:hidden;
  flex-shrink:0;
}
#sidebar-header { padding:12px 14px 10px; border-bottom:1px solid var(--border); }
#sidebar-header h2 { font-size:11px; font-weight:700; text-transform:uppercase; letter-spacing:1.5px; color:var(--muted); margin-bottom:8px; }

.legend-item { display:flex; align-items:flex-start; gap:7px; padding:7px 9px; border-radius:6px; background:var(--surface2); cursor:pointer; transition:all 0.15s; font-size:12px; border:1.5px solid transparent; margin-bottom:4px; }
.legend-item:hover { background:var(--border); }
.legend-item.active-filter { border-color:currentColor; }

#knocks-section { flex:1; overflow-y:auto; padding:8px; }
#knocks-section::-webkit-scrollbar { width:3px; }
#knocks-section::-webkit-scrollbar-thumb { background:var(--border); border-radius:2px; }
.knocks-list-header { display:flex; justify-content:space-between; align-items:center; margin-bottom:7px; padding:0 2px; }
.knocks-list-header h3 { font-size:10px; font-weight:700; text-transform:uppercase; letter-spacing:1px; color:var(--muted); }
#clear-all-btn { font-size:11px; color:var(--muted); background:none; border:none; cursor:pointer; font-family:'DM Sans',sans-serif; padding:2px 5px; border-radius:4px; }
#clear-all-btn:hover { color:#e05c5c; }

.knock-card { background:var(--surface2); border:1px solid var(--border); border-left:3px solid transparent; border-radius:8px; padding:9px 11px; margin-bottom:5px; cursor:pointer; position:relative; }
.knock-card:hover { background:var(--border); }
.knock-card-header { display:flex; justify-content:space-between; align-items:flex-start; gap:8px; }
.knock-address { font-size:12px; font-weight:600; line-height:1.3; flex:1; }
.knock-status-badge { font-size:10px; font-weight:700; padding:2px 6px; border-radius:10px; white-space:nowrap; }
.knock-notes { font-size:11px; color:var(--muted); margin-top:4px; line-height:1.4; }
.knock-time { font-size:10px; color:var(--muted); margin-top:3px; font-family:'Space Mono',monospace; }
.knock-delete { position:absolute; top:5px; right:5px; background:rgba(224,92,92,0.15); color:#e05c5c; border:none; border-radius:4px; padding:2px 5px; font-size:10px; cursor:pointer; display:none; }
.knock-card:hover .knock-delete { display:block; }

#export-btn { margin:0 8px 8px; padding:8px; background:var(--surface2); border:1px solid var(--border); border-radius:8px; color:var(--muted); font-family:'DM Sans',sans-serif; font-size:12px; font-weight:600; cursor:pointer; display:flex; align-items:center; justify-content:center; gap:6px; }

#empty-state { text-align:center; padding:32px 16px; color:var(--muted); }
#empty-state .empty-icon { font-size:28px; margin-bottom:8px; }
#empty-state p { font-size:12px; line-height:1.5; }

/* ═══════════════════════════════
   MOBILE BOTTOM NAV
═══════════════════════════════ */
#mobile-nav {
  display:none;
  position:fixed;
  bottom:0; left:0; right:0;
  background:var(--surface);
  border-top:1px solid var(--border);
  z-index:500;
  height:56px;
  padding-bottom:env(safe-area-inset-bottom);
}
.mobile-nav-btn {
  flex:1;
  height:100%;
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  gap:2px;
  background:none;
  border:none;
  cursor:pointer;
  font-family:'DM Sans',sans-serif;
  color:var(--muted);
  font-size:10px;
  font-weight:600;
}
.mobile-nav-btn.active-nav { color:var(--green); }

/* ═══════════════════════════════
   MODAL
═══════════════════════════════ */
#modal-overlay {
  position:fixed;
  inset:0;
  background:rgba(0,0,0,0.7);
  backdrop-filter:blur(4px);
  z-index:2000;
  display:flex;
  align-items:center;
  justify-content:center;
  opacity:0;
  pointer-events:none;
  transition:opacity 0.2s;
}
#modal-overlay.open { opacity:1; pointer-events:all; }

#modal {
  background:var(--surface);
  border:1px solid var(--border);
  border-radius:16px;
  padding:22px;
  width:380px;
  max-width:94vw;
  max-height:90vh;
  overflow-y:auto;
  transform:scale(0.95);
  transition:transform 0.2s;
}
#modal-overlay.open #modal { transform:scale(1); }
#modal h2 { font-size:16px; font-weight:700; margin-bottom:3px; }
#modal-address { font-size:12px; color:var(--muted); margin-bottom:14px; }

.modal-label { font-size:11px; font-weight:700; text-transform:uppercase; letter-spacing:0.8px; color:var(--muted); margin-bottom:7px; margin-top:13px; display:block; }
.status-grid { display:grid; grid-template-columns:1fr 1fr; gap:7px; }
.status-btn { display:flex; align-items:center; gap:8px; padding:9px 11px; border-radius:9px; border:2px solid var(--border); background:var(--surface2); color:var(--text); font-family:'DM Sans',sans-serif; font-size:13px; font-weight:600; cursor:pointer; text-align:left; }
.status-btn:hover { background:var(--border); }
.status-btn.selected { color:#fff; }

textarea#knock-notes { width:100%; padding:9px 11px; background:var(--surface2); border:1px solid var(--border); border-radius:8px; color:var(--text); font-family:'DM Sans',sans-serif; font-size:13px; resize:vertical; min-height:65px; outline:none; margin-top:2px; }
textarea#knock-notes:focus { border-color:var(--accent); }
#knock-rep-name { width:100%; padding:8px 11px; background:var(--surface2); border:1px solid var(--border); border-radius:8px; color:var(--text); font-family:'DM Sans',sans-serif; font-size:13px; outline:none; margin-top:2px; }
#knock-rep-name:focus { border-color:var(--accent); }

.modal-buttons { display:flex; gap:9px; margin-top:18px; }
#save-knock-btn { flex:1; padding:11px; background:var(--accent); color:#fff; border:none; border-radius:9px; font-family:'DM Sans',sans-serif; font-size:14px; font-weight:700; cursor:pointer; }
#cancel-btn { padding:11px 16px; background:var(--surface2); color:var(--muted); border:1px solid var(--border); border-radius:9px; font-family:'DM Sans',sans-serif; font-size:14px; font-weight:600; cursor:pointer; }

/* autocomplete */
.pac-container { background:var(--surface); border:1px solid var(--border); border-radius:8px; margin-top:4px; font-family:'DM Sans',sans-serif; box-shadow:0 8px 32px rgba(0,0,0,0.4); z-index:9999 !important; }
.pac-item { padding:8px 12px; color:var(--text); font-size:13px; border-top:1px solid var(--border); cursor:pointer; }
.pac-item:first-child { border-top:none; }
.pac-item:hover { background:var(--surface2); }
.pac-item-query { color:var(--text); }
.pac-matched { font-weight:700; color:var(--accent); }
.pac-icon, .hdpi.pac-logo::after, .pac-logo::after { display:none; }

/* ═══════════════════════════════
   BOTTOM SHEET
═══════════════════════════════ */
#bottom-sheet-overlay { position:fixed; inset:0; background:rgba(0,0,0,0.35); z-index:1500; opacity:0; pointer-events:none; transition:opacity 0.25s; }
#bottom-sheet-overlay.open { opacity:1; pointer-events:all; }
#bottom-sheet { position:fixed; bottom:0; left:0; right:0; background:#fff; border-radius:20px 20px 0 0; z-index:1600; padding:0 0 32px; transform:translateY(100%); transition:transform 0.3s cubic-bezier(0.32,0.72,0,1); max-height:72vh; overflow-y:auto; }
#bottom-sheet.open { transform:translateY(0); }
#bs-handle { width:40px; height:5px; background:#ddd; border-radius:3px; margin:12px auto 8px; }
#bs-content { padding:4px 18px 8px; }
.bs-header { display:flex; align-items:center; gap:12px; margin-bottom:5px; }
.bs-header-pin { width:52px; height:52px; border-radius:50%; display:flex; align-items:center; justify-content:center; font-size:24px; flex-shrink:0; color:white; font-weight:900; }
.bs-header-info h2 { font-size:19px; font-weight:800; color:#111; line-height:1.1; }
.bs-status-label { font-size:13px; color:#666; margin-top:2px; }
.bs-see-activity { margin-left:auto; font-size:14px; font-weight:700; color:#4f8ef7; background:none; border:none; cursor:pointer; font-family:'DM Sans',sans-serif; flex-shrink:0; }
.bs-address { font-size:16px; font-weight:500; color:#111; line-height:1.4; margin:10px 0 3px; }
.bs-meta { font-size:12px; color:#888; margin-bottom:3px; }
.bs-notes { font-size:13px; color:#555; background:#f5f5f5; border-radius:8px; padding:7px 11px; margin:7px 0; }
.bs-divider { height:1px; background:#eee; margin:12px 0; }
.bs-status-row { display:flex; gap:10px; overflow-x:auto; padding-bottom:5px; scrollbar-width:none; }
.bs-status-row::-webkit-scrollbar { display:none; }
.bs-status-item { display:flex; flex-direction:column; align-items:center; gap:4px; flex-shrink:0; cursor:pointer; }
.bs-status-btn { width:50px; height:50px; border-radius:50%; display:flex; align-items:center; justify-content:center; font-size:21px; font-weight:900; color:white; border:none; cursor:pointer; transition:transform 0.15s; }
.bs-status-abbr { font-size:10px; font-weight:700; color:#555; letter-spacing:0.4px; font-family:'DM Sans',sans-serif; }
.bs-actions { display:flex; gap:8px; margin-top:14px; }
.bs-edit-btn { flex:1; padding:11px; background:#111; color:#fff; border:none; border-radius:10px; font-family:'DM Sans',sans-serif; font-size:14px; font-weight:700; cursor:pointer; }
.bs-delete-btn { padding:11px 16px; background:#fff; color:#e05c5c; border:1.5px solid #e05c5c; border-radius:10px; font-family:'DM Sans',sans-serif; font-size:14px; font-weight:700; cursor:pointer; }

/* ═══════════════════════════════
   DETAIL PAGE
═══════════════════════════════ */
#detail-page { position:fixed; inset:0; background:#f2f2f7; z-index:2500; display:flex; flex-direction:column; transform:translateX(100%); transition:transform 0.3s cubic-bezier(0.32,0.72,0,1); font-family:'DM Sans',sans-serif; }
#detail-page.open { transform:translateX(0); }
#detail-topbar { display:flex; align-items:center; background:#fff; border-bottom:1px solid #e5e5ea; padding:11px 14px; gap:8px; flex-shrink:0; }
#detail-back { width:34px; height:34px; border-radius:50%; background:#f2f2f7; border:none; cursor:pointer; display:flex; align-items:center; justify-content:center; flex-shrink:0; }
#detail-tabs { display:flex; flex:1; justify-content:center; gap:3px; background:#f2f2f7; border-radius:10px; padding:3px; }
.detail-tab { flex:1; padding:7px 12px; border:none; border-radius:8px; background:transparent; font-family:'DM Sans',sans-serif; font-size:14px; font-weight:600; color:#666; cursor:pointer; }
.detail-tab.active { background:#fff; color:#111; box-shadow:0 1px 4px rgba(0,0,0,0.12); }
#detail-delete-btn { width:34px; height:34px; border-radius:50%; background:#f2f2f7; border:none; cursor:pointer; display:flex; align-items:center; justify-content:center; flex-shrink:0; }
#detail-content { flex:1; overflow-y:auto; }
.detail-save-btn { width:calc(100% - 32px); margin:8px 16px 16px; padding:13px; background:#111; color:#fff; border:none; border-radius:12px; font-family:'DM Sans',sans-serif; font-size:15px; font-weight:700; cursor:pointer; }
.activity-meta-row { display:flex; justify-content:space-between; align-items:center; padding:13px 18px; border-bottom:1px solid #e5e5ea; background:#fff; font-size:13px; color:#888; }
.activity-meta-row span:last-child { color:#111; font-weight:600; }
.activity-item { display:flex; align-items:center; gap:12px; padding:13px 18px; background:#fff; border-bottom:1px solid #f2f2f7; }
.activity-pin-circle { width:42px; height:42px; border-radius:50%; display:flex; align-items:center; justify-content:center; font-size:19px; color:white; font-weight:900; flex-shrink:0; }
.activity-info h3 { font-size:14px; font-weight:700; color:#111; }
.activity-info p { font-size:12px; color:#888; margin-top:1px; }
.activity-map-pin { margin-left:auto; flex-shrink:0; }

/* ═══════════════════════════════
   MOBILE DRAWERS
═══════════════════════════════ */
.mobile-drawer { display:none; position:fixed; inset:0; z-index:1800; }
.mobile-drawer-overlay { position:absolute; inset:0; background:rgba(0,0,0,0.5); }
.mobile-drawer-sheet { position:absolute; bottom:0; left:0; right:0; background:var(--surface); border-radius:20px 20px 0 0; padding:0 14px; padding-bottom:calc(16px + env(safe-area-inset-bottom)); max-height:80vh; overflow-y:auto; }
.mobile-drawer-handle { width:38px; height:5px; background:var(--border); border-radius:3px; margin:11px auto 13px; }

/* ═══════════════════════════════
   RESPONSIVE
═══════════════════════════════ */
@media (max-width: 768px) {
  /* Dashboard */
  #dashboard-topbar { padding:10px 14px; }
  .gn-logo-title { font-size:20px; letter-spacing:3px; }
  .gn-logo-sub { display:none; }
  #open-map-btn { padding:9px 14px; font-size:13px; }
  #dashboard-content { padding:12px; }
  .stats-grid { grid-template-columns:repeat(2,1fr) !important; gap:8px; }
  .stat-card { padding:11px; }
  .stat-card-value { font-size:22px; }
  .time-tab { padding:5px 7px; font-size:10px; }
  #time-filter-tabs { overflow-x:auto; scrollbar-width:none; }
  #time-filter-tabs::-webkit-scrollbar { display:none; }
  .dash-two-col { grid-template-columns:1fr !important; }

  /* Topbar: hide logo, search fills width */
  #topbar-logo { display:none; }
  #stats-bar { display:none; }
  #topbar { padding:8px; }
  #address-search { font-size:16px; } /* 16px prevents iOS zoom on focus */

  /* Sidebar hidden */
  #sidebar { display:none !important; }

  /* Bottom nav visible */
  #mobile-nav { display:flex; }

  /* Map area shrinks above nav */
  #map-shell { bottom:56px; }
  /* (bottom:56px accounts for the nav bar height) */

  /* Map hint above nav */
  #map-hint { bottom:16px; font-size:11px; padding:6px 12px; white-space:normal; text-align:center; max-width:85%; }

  /* Modal: slide up from bottom */
  #modal-overlay { align-items:flex-end; }
  #modal { width:100% !important; max-width:100% !important; border-radius:20px 20px 0 0 !important; padding-bottom:calc(20px + env(safe-area-inset-bottom)); margin:0; max-height:92vh; }
}

@media (min-width:769px) and (max-width:1100px) {
  #sidebar { width:240px; }
  .stats-grid { grid-template-columns:repeat(3,1fr); }
}
</style>
</head>
<body>

<!-- ═══ DASHBOARD ═══ -->
<div id="dashboard">
  <div id="dashboard-topbar">
    <div style="display:flex;align-items:center;gap:12px">
      <svg width="40" height="40" viewBox="0 0 44 44" fill="none">
        <circle cx="22" cy="22" r="22" fill="#0f1117"/>
        <path d="M22 4C14.268 4 8 10.268 8 18c0 11 14 26 14 26S36 29 36 18C36 10.268 29.732 4 22 4z" fill="#22c55e"/>
        <path d="M15 18l5 5 9-9" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
      <div>
        <div class="gn-logo-title"><span class="green">KNOCK</span>&nbsp;<span class="white">TRACK</span></div>
        <div class="gn-logo-line"></div>
        <div class="gn-logo-sub">by Green Nova · Community Advocates</div>
      </div>
    </div>
    <button id="open-map-btn">🗺️ Open Map</button>
  </div>

  <!-- Rep + Time Filter -->
  <div style="display:flex;align-items:center;gap:10px;padding:12px 20px;background:var(--surface);border-bottom:1px solid var(--border);flex-wrap:wrap">
    <div style="display:flex;align-items:center;gap:8px;background:var(--surface2);border:1px solid var(--border);border-radius:8px;padding:8px 12px;flex:1;min-width:160px">
      <span style="font-size:13px;color:var(--muted);font-weight:600;white-space:nowrap">👤 Rep:</span>
      <input type="text" id="dashboard-rep-name" placeholder="Filter your stats by name…" style="background:none;border:none;outline:none;color:var(--text);font-family:'DM Sans',sans-serif;font-size:13px;font-weight:600;flex:1;min-width:0"/>
    </div>
    <div id="time-filter-tabs" style="display:flex;gap:4px;background:var(--surface2);border:1px solid var(--border);border-radius:8px;padding:3px;flex-shrink:0">
      <button class="time-tab active-time" data-period="today" onclick="setTimePeriod('today')">Today</button>
      <button class="time-tab" data-period="week" onclick="setTimePeriod('week')">Week</button>
      <button class="time-tab" data-period="month" onclick="setTimePeriod('month')">Month</button>
      <button class="time-tab" data-period="year" onclick="setTimePeriod('year')">Year</button>
      <button class="time-tab" data-period="all" onclick="setTimePeriod('all')">All Time</button>
    </div>
  </div>

  <div id="dashboard-content">
    <div class="dash-section-title" onclick="toggleSection('my-perf')">
      <div class="section-dot" style="background:var(--green)"></div>My Performance
      <span class="collapse-arrow" id="arrow-my-perf">▼</span>
    </div>
    <div id="my-perf" class="collapsible-body collapsed">
      <div id="my-stats-grid" class="stats-grid"></div>
    </div>

    <div class="dash-section-title" onclick="toggleSection('my-disp')">
      <div class="section-dot" style="background:var(--green)"></div>My Lead Dispositions
      <span class="collapse-arrow" id="arrow-my-disp">▼</span>
    </div>
    <div id="my-disp" class="collapsible-body collapsed">
      <div class="disposition-table" id="my-dispositions" style="margin-bottom:14px"></div>
    </div>

    <div class="dash-section-title" onclick="toggleSection('co-perf')">
      <div class="section-dot" style="background:var(--accent)"></div>Company Wide
      <span class="collapse-arrow" id="arrow-co-perf">▼</span>
    </div>
    <div id="co-perf" class="collapsible-body collapsed">
      <div id="company-stats-grid" class="stats-grid"></div>
    </div>

    <div class="dash-section-title" onclick="toggleSection('co-disp')">
      <div class="section-dot" style="background:var(--accent)"></div>Company Lead Dispositions
      <span class="collapse-arrow" id="arrow-co-disp">▼</span>
    </div>
    <div id="co-disp" class="collapsible-body collapsed">
      <div class="disposition-table" id="company-dispositions" style="margin-bottom:40px"></div>
    </div>
  </div>
</div>

<!-- ═══ MAP SHELL ═══ -->
<div id="map-shell">
  <!-- Topbar -->
  <div id="topbar">
    <div id="topbar-logo" style="display:flex;align-items:center;gap:7px">
      <svg width="24" height="24" viewBox="0 0 44 44" fill="none">
        <path d="M22 2C13.163 2 6 9.163 6 18c0 12 16 28 16 28S38 30 38 18C38 9.163 30.837 2 22 2z" fill="#22c55e"/>
        <path d="M14 18l6 6 10-10" stroke="white" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
      <span style="font-family:'Rajdhani',sans-serif;font-size:17px;letter-spacing:3px;font-weight:700"><span style="color:var(--green)">KNOCK</span> <span style="color:var(--text)">TRACK</span></span>
    </div>
    <div id="search-wrap">
      <span class="search-icon">🔍</span>
      <input id="address-search" type="text" placeholder="Search address…" autocomplete="off"/>
    </div>
    <div id="stats-bar">
      <button onclick="showDashboard()" style="padding:5px 11px;background:var(--surface2);border:1px solid var(--border);border-radius:20px;color:var(--muted);font-family:'DM Sans',sans-serif;font-size:12px;font-weight:600;cursor:pointer">🏠 Home</button>
      <div class="stat-pill"><div class="stat-dot" style="background:#56e89c"></div><span id="stat-leads">0</span> Leads</div>
      <div class="stat-pill"><div class="stat-dot" style="background:#e05c5c"></div><span id="stat-ni">0</span> NI</div>
      <div class="stat-pill"><div class="stat-dot" style="background:var(--muted)"></div><span id="stat-total">0</span> Total</div>
    </div>
  </div>

  <!-- Map + Sidebar -->
  <div id="map-and-sidebar">
    <div id="map-container">
      <div id="google-map"></div>
      <div id="map-hint" style="display:none">📍 Tap anywhere on the map to drop a pin</div>

      <!-- Top right controls -->
      <div id="map-type-toggle">
        <button id="pin-drop-btn" onclick="togglePinMode()" style="padding:7px 14px;border-radius:8px;border:2px solid transparent;font-family:'DM Sans',sans-serif;font-size:12px;font-weight:700;cursor:pointer;backdrop-filter:blur(8px);background:rgba(26,29,39,0.85);color:#aaa;border-color:rgba(255,255,255,0.1);display:flex;align-items:center;gap:5px">
          📍 Drop Pin
        </button>
        <button class="map-type-btn active-type" id="btn-roadmap" onclick="setMapType('roadmap')">🗺️ Map</button>
        <button class="map-type-btn inactive" id="btn-hybrid" onclick="setMapType('hybrid')">🛰️ Satellite</button>
      </div>

      <!-- Bottom left controls -->
      <div style="position:absolute;bottom:90px;left:12px;z-index:10;display:flex;flex-direction:column;gap:6px">
        <button onclick="goToMyLocation()" title="My Location" style="width:40px;height:40px;border-radius:10px;border:none;background:rgba(26,29,39,0.9);backdrop-filter:blur(8px);color:#fff;font-size:18px;cursor:pointer;display:flex;align-items:center;justify-content:center;box-shadow:0 2px 8px rgba(0,0,0,0.4)">🧭</button>
        <button id="route-btn" onclick="toggleRouteTracker()" title="Route Tracker" style="width:40px;height:40px;border-radius:10px;border:none;background:rgba(26,29,39,0.9);backdrop-filter:blur(8px);color:#fff;font-size:18px;cursor:pointer;display:flex;align-items:center;justify-content:center;box-shadow:0 2px 8px rgba(0,0,0,0.4)">⏱️</button>
        <button id="heatmap-btn" onclick="toggleHeatmap()" title="Heat Map" style="width:40px;height:40px;border-radius:10px;border:none;background:rgba(26,29,39,0.9);backdrop-filter:blur(8px);color:#fff;font-size:18px;cursor:pointer;display:flex;align-items:center;justify-content:center;box-shadow:0 2px 8px rgba(0,0,0,0.4)">📊</button>
      </div>

      <!-- Route status badge -->
      <div id="route-badge" style="display:none;position:absolute;bottom:90px;left:60px;z-index:10;background:#22c55e;color:#000;font-size:12px;font-weight:700;padding:5px 12px;border-radius:20px;font-family:'DM Sans',sans-serif;backdrop-filter:blur(8px)">⏱️ Tracking route…</div>
    </div>

    <div id="sidebar">
      <div id="sidebar-header">
        <h2>Status Legend</h2>
        <div id="legend-grid"></div>
      </div>
      <div id="knocks-section">
        <div class="knocks-list-header">
          <h3>Recent Knocks</h3>
          <button id="clear-all-btn">Clear All</button>
        </div>
        <div id="knocks-list"></div>
        <div id="empty-state">
          <div class="empty-icon">🚪</div>
          <p>Click on the map to log your first knock!</p>
        </div>
      </div>
      <button id="export-btn">📊 Export CSV</button>
    </div>
  </div>
</div>

<!-- ═══ MOBILE BOTTOM NAV ═══ -->
<div id="mobile-nav">
  <button class="mobile-nav-btn active-nav" onclick="mobileNav('map')">
    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 11l19-9-9 19-2-8-8-2z"/></svg>
    Map
  </button>
  <button class="mobile-nav-btn" onclick="mobileNav('legend')">
    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="3"/><circle cx="12" cy="5" r="1.5"/><circle cx="12" cy="19" r="1.5"/><circle cx="5" cy="12" r="1.5"/><circle cx="19" cy="12" r="1.5"/></svg>
    Legend
  </button>
  <button class="mobile-nav-btn" onclick="mobileNav('knocks')">
    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M14 2H6a2 2 0 00-2 2v16a2 2 0 002 2h12a2 2 0 002-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>
    Knocks
  </button>
  <button class="mobile-nav-btn" onclick="showDashboard()">
    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/><rect x="14" y="14" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/></svg>
    Stats
  </button>
</div>

<!-- ═══ MODAL ═══ -->
<div id="modal-overlay">
  <div id="modal">
    <h2>Log a Knock</h2>
    <div id="modal-address">Getting address…</div>
    <span class="modal-label">Outcome</span>
    <div class="status-grid" id="status-grid"></div>
    <span class="modal-label">Rep Name</span>
    <input type="text" id="knock-rep-name" placeholder="Your name"/>
    <span class="modal-label">Notes (optional)</span>
    <textarea id="knock-notes" placeholder="e.g. Call back Tuesday 6pm"></textarea>
    <div class="modal-buttons">
      <button id="cancel-btn">Cancel</button>
      <button id="save-knock-btn">Save Knock</button>
    </div>
  </div>
</div>

<!-- ═══ BOTTOM SHEET ═══ -->
<div id="bottom-sheet-overlay" onclick="closeBottomSheet()"></div>
<div id="bottom-sheet">
  <div id="bs-handle"></div>
  <div id="bs-content"></div>
</div>

<!-- ═══ DETAIL PAGE ═══ -->
<div id="detail-page">
  <div id="detail-topbar">
    <button id="detail-back" onclick="closeDetailPage()">
      <svg width="10" height="18" viewBox="0 0 10 18" fill="none"><path d="M9 1L1 9L9 17" stroke="#111" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
    </button>
    <div id="detail-tabs">
      <button class="detail-tab active" onclick="switchTab('info')">Info</button>
      <button class="detail-tab" onclick="switchTab('activity')">Activity</button>
    </div>
    <button id="detail-delete-btn" onclick="deleteCurrentKnock()">
      <svg width="18" height="20" viewBox="0 0 18 20" fill="none"><path d="M1 4h16M6 4V2h6v2M7 9v6M11 9v6M2 4l1 14a1 1 0 001 1h10a1 1 0 001-1l1-14" stroke="#888" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/></svg>
    </button>
  </div>
  <div id="detail-content"></div>
</div>

<!-- ═══ MOBILE LEGEND DRAWER ═══ -->
<div id="mobile-legend" class="mobile-drawer">
  <div class="mobile-drawer-overlay" onclick="closeMobileDrawer('legend')"></div>
  <div class="mobile-drawer-sheet">
    <div class="mobile-drawer-handle"></div>
    <div style="font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:1.5px;color:var(--muted);margin-bottom:10px">Status Legend</div>
    <div id="mobile-legend-grid"></div>
    <div style="height:8px"></div>
  </div>
</div>

<!-- ═══ MOBILE KNOCKS DRAWER ═══ -->
<div id="mobile-knocks" class="mobile-drawer">
  <div class="mobile-drawer-overlay" onclick="closeMobileDrawer('knocks')"></div>
  <div class="mobile-drawer-sheet">
    <div class="mobile-drawer-handle"></div>
    <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:10px">
      <div style="font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:1.5px;color:var(--muted)">Recent Knocks</div>
      <button onclick="closeMobileDrawer('knocks')" style="background:none;border:none;color:var(--muted);font-size:18px;cursor:pointer;line-height:1">✕</button>
    </div>
    <div id="mobile-knocks-list"></div>
    <div style="height:8px"></div>
  </div>
</div>

<script>
const STATUSES = [
  { key:'enrolled',       label:'Enrolled',       icon:'☀', color:'#22c55e', desc:'Customer has successfully signed up and enrolled.' },
  { key:'not-home',       label:'Not Home',        icon:'⌂', color:'#ef4444', desc:'Nobody answered the door, homeowner was not home.' },
  { key:'quality-lead',   label:'Quality Lead',    icon:'↻', color:'#38bdf8', desc:'Highly interested homeowner, strong chance to close.' },
  { key:'lead',           label:'Lead',            icon:'📍', color:'#f97316', desc:'Homeowner showed interest, follow up needed.' },
  { key:'missed-lead',    label:'Missed Lead',     icon:'📅', color:'#991b1b', desc:'Appointment was missed or lead was cancelled.' },
  { key:'not-interested', label:'Not Interested',  icon:'✕', color:'#94a3b8', desc:'Homeowner declined and is not interested.' },
  { key:'not-qualified',  label:'Not Qualified',   icon:'👎', color:'#6b7280', desc:'Homeowner does not meet the qualification criteria.' },
  { key:'competitor',     label:'Competitor',      icon:'✕', color:'#f43f5e', desc:'Homeowner is already with a competing provider.' },
  { key:'jerk',           label:'Jerk',            icon:'😠', color:'#b91c1c', desc:'Hostile or rude homeowner, do not revisit.' },
  { key:'solar-roof',     label:'Solar on Roof',   icon:'☀', color:'#fbbf24', desc:'Home already has solar panels installed on the roof.' },
  { key:'spanish',        label:'Spanish Only',    icon:'💬', color:'#06b6d4', desc:'Homeowner speaks Spanish only, send a Spanish-speaking rep.' },
];

const ABBREVS = { 'not-home':'NOT','enrolled':'ENR','quality-lead':'QL','lead':'LEAD','missed-lead':'MISS','not-interested':'NI','not-qualified':'NQ','competitor':'COMP','jerk':'JERK','solar-roof':'SOLAR','spanish':'ESP' };
const NON_INTERACTIONS = new Set(['not-home','not-qualified','competitor','spanish']);

let map, geocoder, infoWindow, autocomplete;
// ── SUPABASE ──
const SUPABASE_URL = 'https://icjbgvkxwplgzuksyrig.supabase.co';
const SUPABASE_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImljamJndmt4d3BsZ3p1a3N5cmlnIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzI2ODUxNjEsImV4cCI6MjA4ODI2MTE2MX0.ULEiqRvyGGt0dWsS1Qr-DuW9T4Vx_aVRPxpA2yWkEeU';

async function sbFetch(path, options = {}) {
  const res = await fetch(`${SUPABASE_URL}/rest/v1/${path}`, {
    ...options,
    headers: {
      'apikey': SUPABASE_KEY,
      'Authorization': `Bearer ${SUPABASE_KEY}`,
      'Content-Type': 'application/json',
      'Prefer': options.prefer || 'return=representation',
      ...(options.headers || {})
    }
  });
  if (!res.ok) { const err = await res.text(); console.error('Supabase error:', err); return null; }
  const text = await res.text();
  return text ? JSON.parse(text) : null;
}

function knockToRow(k) {
  return {
    id: k.id, lat: k.lat, lng: k.lng, address: k.address,
    status: k.status, notes: k.notes || null, rep: k.rep || null,
    time: k.time, created: k.created || k.time,
    history: k.history || [],
    appointment: k.appointment || null,
    business: k.business || null,
    first_name: k.firstName || null, last_name: k.lastName || null,
    phone: k.phone || null, phone2: k.phone2 || null,
    email: k.email || null, apt: k.apt || null,
    developer: k.developer || null, annual_kwh: k.annualKwh || null,
    utility: k.utility || null,
  };
}

function rowToKnock(r) {
  return {
    id: r.id, lat: r.lat, lng: r.lng, address: r.address,
    status: r.status, notes: r.notes || '', rep: r.rep || '',
    time: r.time, created: r.created || r.time,
    history: r.history || [],
    appointment: r.appointment || null,
    business: r.business || '', firstName: r.first_name || '',
    lastName: r.last_name || '', phone: r.phone || '',
    phone2: r.phone2 || '', email: r.email || '',
    apt: r.apt || '', developer: r.developer || '',
    annualKwh: r.annual_kwh || '', utility: r.utility || '',
  };
}

async function loadKnocks() {
  showSyncStatus('loading');
  const rows = await sbFetch('knocks?order=time.desc&limit=2000');
  if (rows) {
    knocks = rows.map(rowToKnock);
    renderMarkers(); renderList(); updateStats();
  }
  hideSyncStatus();
}

async function saveKnock(knock) {
  showSyncStatus('saving');
  const row = knockToRow(knock);
  await sbFetch('knocks', {
    method: 'POST',
    prefer: 'return=minimal',
    body: JSON.stringify(row),
    headers: { 'Prefer': 'resolution=merge-duplicates,return=minimal' }
  });
  hideSyncStatus();
}

async function updateKnock(knock) {
  showSyncStatus('saving');
  const row = knockToRow(knock);
  await sbFetch(`knocks?id=eq.${knock.id}`, {
    method: 'PATCH',
    prefer: 'return=minimal',
    body: JSON.stringify(row)
  });
  hideSyncStatus();
}

async function removeKnock(id) {
  await sbFetch(`knocks?id=eq.${id}`, { method: 'DELETE', prefer: 'return=minimal' });
}

function showSyncStatus(state) {
  let el = document.getElementById('sync-status');
  if (!el) {
    el = document.createElement('div');
    el.id = 'sync-status';
    el.style.cssText = 'position:fixed;top:8px;right:8px;z-index:9999;padding:5px 12px;border-radius:20px;font-size:12px;font-weight:700;font-family:DM Sans,sans-serif;transition:opacity 0.3s;pointer-events:none';
    document.body.appendChild(el);
  }
  if (state === 'loading') { el.style.background = '#4f8ef7'; el.style.color = '#fff'; el.textContent = '⟳ Loading…'; el.style.opacity = '1'; }
  else if (state === 'saving') { el.style.background = '#22c55e'; el.style.color = '#000'; el.textContent = '✓ Saving…'; el.style.opacity = '1'; }
}
function hideSyncStatus() {
  const el = document.getElementById('sync-status');
  if (el) setTimeout(() => { el.style.opacity = '0'; }, 800);
}

// Poll for new knocks from other reps every 15 seconds
function startPolling() {
  setInterval(async () => {
    const rows = await sbFetch('knocks?order=time.desc&limit=2000');
    if (rows) {
      knocks = rows.map(rowToKnock);
      renderMarkers(); renderList(); updateStats();
      if (heatmapActive) toggleHeatmap(); // refresh heatmap data
    }
  }, 15000);
}

let knocks = [];
let markers = {};
let pendingLat = null, pendingLng = null;
let selectedStatus = null;
let activeFilter = null;
let isDragging = false;
let pinModeActive = false;
let editingId = null;
let currentDetailId = null;
let currentDetailTab = 'info';
let currentTimePeriod = 'all';

// ── Build Legend ──
const legendGrid = document.getElementById('legend-grid');
STATUSES.forEach(s => {
  const el = document.createElement('div');
  el.className = 'legend-item';
  el.style.color = s.color;
  el.innerHTML = `<div style="display:flex;align-items:center;gap:7px;width:100%">
    <div style="background:${s.color};color:#fff;font-size:13px;width:26px;height:26px;border-radius:50%;display:flex;align-items:center;justify-content:center;flex-shrink:0;font-weight:900">${s.icon}</div>
    <div>
      <div style="font-size:12px;font-weight:700;color:var(--text)">${s.label}</div>
      <div style="font-size:10px;color:var(--muted);line-height:1.3;margin-top:1px">${s.desc}</div>
    </div>
  </div>`;
  el.dataset.key = s.key;
  el.addEventListener('click', () => toggleFilter(s.key, el));
  legendGrid.appendChild(el);
});

// ── Build Status Buttons in Modal ──
const statusGrid = document.getElementById('status-grid');
STATUSES.forEach(s => {
  const btn = document.createElement('button');
  btn.className = 'status-btn';
  btn.dataset.key = s.key;
  btn.innerHTML = `<div style="width:26px;height:26px;border-radius:50%;background:${s.color};display:flex;align-items:center;justify-content:center;color:#fff;font-size:13px;font-weight:900;flex-shrink:0">${s.icon}</div><span>${s.label}</span>`;
  btn.addEventListener('click', () => selectStatus(s.key, btn));
  statusGrid.appendChild(btn);
});

function selectStatus(key, btn) {
  selectedStatus = key;
  document.querySelectorAll('.status-btn').forEach(b => {
    b.classList.remove('selected');
    b.style.borderColor = 'var(--border)';
    b.style.background = 'var(--surface2)';
  });
  const s = STATUSES.find(x => x.key === key);
  btn.classList.add('selected');
  btn.style.borderColor = s.color;
  btn.style.background = s.color + '28';
}

function toggleFilter(key, el) {
  if (activeFilter === key) {
    activeFilter = null;
    document.querySelectorAll('.legend-item').forEach(x => x.classList.remove('active-filter'));
  } else {
    activeFilter = key;
    document.querySelectorAll('.legend-item').forEach(x => x.classList.remove('active-filter'));
    el.classList.add('active-filter');
  }
  renderMarkers();
  renderList();
}

// ── INIT MAP ──
async function initMap() {
  map = new google.maps.Map(document.getElementById('google-map'), {
    center: { lat:39.5, lng:-98.35 },
    zoom: 5,
    styles: darkMapStyle(),
    disableDefaultUI: false,
    zoomControl: true,
    mapTypeControl: false,
    streetViewControl: false,
    fullscreenControl: false,
  });
  geocoder = new google.maps.Geocoder();
  infoWindow = new google.maps.InfoWindow();

  const searchInput = document.getElementById('address-search');
  autocomplete = new google.maps.places.Autocomplete(searchInput, { componentRestrictions:{ country:'us' } });
  autocomplete.bindTo('bounds', map);

  autocomplete.addListener('place_changed', () => {
    const place = autocomplete.getPlace();
    if (!place.geometry || !place.geometry.location) {
      geocodeAndOpen(searchInput.value);
      return;
    }
    const loc = place.geometry.location;
    if (place.geometry.viewport) map.fitBounds(place.geometry.viewport);
    else map.setCenter(loc);
    map.setZoom(18);
    searchInput.blur();
    setTimeout(() => openModal(loc.lat(), loc.lng(), null, place.formatted_address), 400);
  });

  searchInput.addEventListener('keydown', e => {
    if (e.key === 'Enter') { e.preventDefault(); geocodeAndOpen(searchInput.value); }
  });

  map.addListener('click', e => {
    if (isDragging) { isDragging = false; return; }
    if (!pinModeActive) return;
    closeBottomSheet();
    const existing = checkAlreadyKnocked(e.latLng.lat(), e.latLng.lng());
    if (existing) {
      const s = STATUSES.find(x => x.key === existing.status);
      if (!confirm(`⚠️ Already knocked!\n\n${existing.address}\nStatus: ${s ? s.label : existing.status}\nRep: ${existing.rep || 'Unknown'}\n\nDo you want to update this knock?`)) {
        togglePinMode(false); return;
      }
      openModal(e.latLng.lat(), e.latLng.lng(), existing.id);
    } else {
      openModal(e.latLng.lat(), e.latLng.lng());
    }
    togglePinMode(false);
  });

  await loadKnocks();
  startPolling();
}

function geocodeAndOpen(val) {
  if (!val.trim()) return;
  geocoder.geocode({ address: val }, (results, status) => {
    if (status === 'OK' && results[0]) {
      const loc = results[0].geometry.location;
      if (results[0].geometry.viewport) map.fitBounds(results[0].geometry.viewport);
      else map.setCenter(loc);
      map.setZoom(18);
      document.getElementById('address-search').blur();
      setTimeout(() => openModal(loc.lat(), loc.lng(), null, results[0].formatted_address), 400);
    }
  });
}

function openModal(lat, lng, existingId = null, prefillAddress = null) {
  pendingLat = lat; pendingLng = lng; editingId = existingId; selectedStatus = null;
  document.getElementById('modal-overlay').classList.add('open');
  document.getElementById('modal-address').textContent = 'Getting address…';
  document.querySelectorAll('.status-btn').forEach(b => {
    b.classList.remove('selected');
    b.style.borderColor = 'var(--border)';
    b.style.background = 'var(--surface2)';
  });
  document.getElementById('knock-notes').value = '';
  document.getElementById('knock-rep-name').value = window._defaultRep || '';

  if (existingId) {
    const ex = knocks.find(k => k.id === existingId);
    if (ex) {
      selectedStatus = ex.status;
      document.getElementById('knock-notes').value = ex.notes || '';
      document.getElementById('knock-rep-name').value = ex.rep || '';
      const btn = document.querySelector(`.status-btn[data-key="${ex.status}"]`);
      if (btn) selectStatus(ex.status, btn);
    }
  }
  if (prefillAddress) {
    document.getElementById('modal-address').textContent = prefillAddress;
  } else {
    geocoder.geocode({ location:{ lat, lng } }, (results, status) => {
      document.getElementById('modal-address').textContent =
        (status === 'OK' && results[0]) ? results[0].formatted_address : `${lat.toFixed(5)}, ${lng.toFixed(5)}`;
    });
  }
}

document.getElementById('cancel-btn').addEventListener('click', closeModal);
document.getElementById('modal-overlay').addEventListener('click', e => {
  if (e.target === document.getElementById('modal-overlay')) closeModal();
});
function closeModal() {
  document.getElementById('modal-overlay').classList.remove('open');
  pendingLat = null; pendingLng = null; editingId = null;
}

document.getElementById('save-knock-btn').addEventListener('click', async () => {
  if (!selectedStatus) {
    document.getElementById('status-grid').style.outline = '1px solid #e05c5c';
    setTimeout(() => document.getElementById('status-grid').style.outline = '', 1500);
    return;
  }
  const address = document.getElementById('modal-address').textContent;
  const notes = document.getElementById('knock-notes').value.trim();
  const rep = document.getElementById('knock-rep-name').value.trim();

  if (editingId) {
    const idx = knocks.findIndex(k => k.id === editingId);
    if (idx !== -1) {
      knocks[idx].status = selectedStatus; knocks[idx].notes = notes; knocks[idx].rep = rep;
      if (markers[editingId]) markers[editingId].setIcon(makeMarkerIcon(selectedStatus));
      await updateKnock(knocks[idx]);
    }
  } else {
    const knock = { id: Date.now().toString(), lat: pendingLat, lng: pendingLng, address, status: selectedStatus, notes, rep, time: new Date().toISOString(), created: new Date().toISOString(), history: [] };
    knocks.unshift(knock);
    addMarker(knock);
    await saveKnock(knock);
  }
  renderList(); updateStats(); closeModal();
});

function addMarker(knock) {
  const marker = new google.maps.Marker({
    position:{ lat:knock.lat, lng:knock.lng }, map,
    icon:makeMarkerIcon(knock.status), title:getStatusLabel(knock.status),
    animation:google.maps.Animation.DROP, draggable:true,
  });
  marker.addListener('click', () => openBottomSheet(knock));
  marker.addListener('dragstart', () => { isDragging = true; marker.setOpacity(0.7); });
  marker.addListener('dragend', e => {
    isDragging = true;
    setTimeout(() => { isDragging = false; }, 300);
    marker.setOpacity(1);
    const newLat = e.latLng.lat(), newLng = e.latLng.lng();
    const idx = knocks.findIndex(k => k.id === knock.id);
    if (idx !== -1) {
      knocks[idx].lat = newLat; knocks[idx].lng = newLng;
      geocoder.geocode({ location:{ lat:newLat, lng:newLng } }, (results, status) => {
        const addr = (status === 'OK' && results[0]) ? results[0].formatted_address : `${newLat.toFixed(5)}, ${newLng.toFixed(5)}`;
        knocks[idx].address = addr; knock.address = addr;
        updateKnock(knocks[idx]); renderList();
      });
    }
  });
  markers[knock.id] = marker;
}

window.quickStatus = function(knockId, statusKey) {
  const idx = knocks.findIndex(k => k.id === knockId);
  if (idx !== -1) {
    if (!knocks[idx].history) knocks[idx].history = [];
    knocks[idx].history.unshift({ status:knocks[idx].status, time:new Date().toISOString() });
    knocks[idx].status = statusKey;
    if (markers[knockId]) markers[knockId].setIcon(makeMarkerIcon(statusKey));
    updateKnock(knocks[idx]);
    renderList(); updateStats();
    document.querySelectorAll('.bs-status-btn').forEach(b => {
      b.style.transform = b.dataset.key === statusKey ? 'scale(1.15)' : 'scale(1)';
      b.style.boxShadow = b.dataset.key === statusKey ? '0 0 0 3px white' : 'none';
    });
    const s = STATUSES.find(x => x.key === statusKey);
    const hp = document.getElementById('bs-header-pin');
    const hs = document.getElementById('bs-header-status');
    if (hp) { hp.style.background = s.color; hp.textContent = s.icon; }
    if (hs) hs.textContent = s.label;
  }
};

function makeMarkerIcon(statusKey) {
  const s = STATUSES.find(x => x.key === statusKey);
  const color = s ? s.color : '#888';
  const icons = {
    'enrolled':`<text x="18" y="23" font-size="20" text-anchor="middle" font-family="Arial" fill="white">☀</text>`,
    'not-home':`<text x="18" y="23" font-size="21" text-anchor="middle" font-family="Arial" fill="white">⌂</text>`,
    'quality-lead':`<text x="18" y="24" font-size="22" text-anchor="middle" font-family="Arial" fill="white" font-weight="bold">↻</text>`,
    'lead':`<text x="18" y="21" font-size="14" text-anchor="middle" font-family="Arial" fill="white">📍</text>`,
    'missed-lead':`<text x="18" y="21" font-size="14" text-anchor="middle" font-family="Arial" fill="white">📅</text>`,
    'not-interested':`<text x="18" y="22" font-size="17" text-anchor="middle" font-family="Arial" fill="white" font-weight="bold">✕</text>`,
    'not-qualified':`<text x="18" y="21" font-size="14" text-anchor="middle" font-family="Arial" fill="white">👎</text>`,
    'competitor':`<text x="18" y="22" font-size="17" text-anchor="middle" font-family="Arial" fill="white" font-weight="bold">✕</text>`,
    'jerk':`<text x="18" y="21" font-size="14" text-anchor="middle" font-family="Arial" fill="white">😠</text>`,
    'solar-roof':`<text x="18" y="23" font-size="20" text-anchor="middle" font-family="Arial" fill="white">☀</text>`,
    'spanish':`<text x="18" y="21" font-size="14" text-anchor="middle" font-family="Arial" fill="white">💬</text>`,
  };
  const svg = `<svg xmlns="http://www.w3.org/2000/svg" width="36" height="46" viewBox="0 0 36 46">
    <ellipse cx="18" cy="44" rx="7" ry="3" fill="rgba(0,0,0,0.3)"/>
    <path d="M18 0C10.268 0 4 6.268 4 14c0 9.333 14 28 14 28S32 23.333 32 14C32 6.268 25.732 0 18 0z" fill="${color}" stroke="rgba(255,255,255,0.5)" stroke-width="1.5"/>
    <circle cx="18" cy="14" r="10" fill="rgba(0,0,0,0.15)"/>
    ${icons[statusKey] || ''}
  </svg>`;
  return { url:`data:image/svg+xml;charset=UTF-8,${encodeURIComponent(svg)}`, scaledSize:new google.maps.Size(36,46), anchor:new google.maps.Point(18,46) };
}

function getStatusLabel(key) { const s = STATUSES.find(x => x.key === key); return s ? s.label : key; }

function renderMarkers() {
  Object.values(markers).forEach(m => m.setMap(null));
  markers = {};
  knocks.forEach(k => { if (!activeFilter || k.status === activeFilter) addMarker(k); });
}

function renderList() {
  const list = document.getElementById('knocks-list');
  const empty = document.getElementById('empty-state');
  const filtered = activeFilter ? knocks.filter(k => k.status === activeFilter) : knocks;
  if (filtered.length === 0) { list.innerHTML = ''; empty.style.display = 'block'; return; }
  empty.style.display = 'none';
  list.innerHTML = filtered.map(k => {
    const s = STATUSES.find(x => x.key === k.status);
    const color = s ? s.color : '#888';
    const timeStr = new Date(k.time).toLocaleTimeString([], { hour:'2-digit', minute:'2-digit' });
    const dateStr = new Date(k.time).toLocaleDateString([], { month:'short', day:'numeric' });
    return `<div class="knock-card" style="border-left-color:${color}" onclick="flyToKnock('${k.id}')">
      <button class="knock-delete" onclick="event.stopPropagation();deleteKnock('${k.id}')">✕</button>
      <div class="knock-card-header">
        <div class="knock-address">${k.address.split(',').slice(0,2).join(',')}</div>
        <div class="knock-status-badge" style="background:${color}22;color:${color}">${s ? s.icon+' '+s.label : k.status}</div>
      </div>
      ${k.rep ? `<div class="knock-notes">👤 ${k.rep}</div>` : ''}
      ${k.notes ? `<div class="knock-notes">${k.notes}</div>` : ''}
      <div class="knock-time">${dateStr} · ${timeStr}</div>
    </div>`;
  }).join('');
}

window.flyToKnock = function(id) {
  const k = knocks.find(x => x.id === id);
  if (k && map) { map.panTo({ lat:k.lat, lng:k.lng }); map.setZoom(18); if (markers[id]) google.maps.event.trigger(markers[id], 'click'); }
};

window.deleteKnock = function(id) {
  knocks = knocks.filter(k => k.id !== id);
  if (markers[id]) { markers[id].setMap(null); delete markers[id]; }
  removeKnock(id);
  renderList(); updateStats();
};

document.getElementById('clear-all-btn').addEventListener('click', async () => {
  if (!confirm('Clear ALL knocks for the entire team?')) return;
  knocks = [];
  Object.values(markers).forEach(m => m.setMap(null));
  markers = {};
  await sbFetch('knocks?id=neq.NONE', { method: 'DELETE', prefer: 'return=minimal' });
  renderList(); updateStats();
});

function updateStats() {
  const leads = knocks.filter(k => ['quality-lead','lead','enrolled'].includes(k.status)).length;
  const ni = knocks.filter(k => ['not-interested','not-home'].includes(k.status)).length;
  document.getElementById('stat-leads').textContent = leads;
  document.getElementById('stat-ni').textContent = ni;
  document.getElementById('stat-total').textContent = knocks.length;
}


document.getElementById('export-btn').addEventListener('click', () => {
  const csv = [['Address','Status','Rep','Notes','Date','Lat','Lng'],
    ...knocks.map(k => [`"${k.address}"`, getStatusLabel(k.status), `"${k.rep||''}"`, `"${(k.notes||'').replace(/"/g,'""')}"`, new Date(k.time).toLocaleString(), k.lat, k.lng])
  ].map(r => r.join(',')).join('\n');
  const a = document.createElement('a');
  a.href = 'data:text/csv;charset=utf-8,' + encodeURIComponent(csv);
  a.download = `knockmap_${new Date().toISOString().slice(0,10)}.csv`;
  a.click();
});

window.togglePinMode = function(force) {
  pinModeActive = force !== undefined ? force : !pinModeActive;
  const btn = document.getElementById('pin-drop-btn');
  const hint = document.getElementById('map-hint');
  if (pinModeActive) {
    btn.style.background = '#22c55e';
    btn.style.color = '#000';
    btn.style.borderColor = '#22c55e';
    btn.textContent = '✕ Cancel';
    if (map) map.setOptions({ draggableCursor:'crosshair' });
    hint.textContent = '📍 Tap anywhere on the map to drop a pin';
    hint.style.display = 'block';
  } else {
    btn.style.background = 'rgba(26,29,39,0.85)';
    btn.style.color = '#aaa';
    btn.style.borderColor = 'rgba(255,255,255,0.1)';
    btn.innerHTML = '📍 Drop Pin';
    if (map) map.setOptions({ draggableCursor:null });
    hint.style.display = 'none';
  }
};

window.setMapType = function(type) {
  if (!map) return;
  if (type === 'hybrid') { map.setMapTypeId(google.maps.MapTypeId.HYBRID); map.setOptions({ styles:[] }); }
  else { map.setMapTypeId(google.maps.MapTypeId.ROADMAP); map.setOptions({ styles:darkMapStyle() }); }
  document.getElementById('btn-roadmap').className = 'map-type-btn ' + (type === 'roadmap' ? 'active-type' : 'inactive');
  document.getElementById('btn-hybrid').className = 'map-type-btn ' + (type === 'hybrid' ? 'active-type' : 'inactive');
};

// ── BOTTOM SHEET ──
function openBottomSheet(knock) {
  const s = STATUSES.find(x => x.key === knock.status) || STATUSES[0];
  const addrParts = knock.address.split(',');
  const line1 = addrParts.slice(0,2).join(',').trim();
  const line2 = addrParts.slice(2).join(',').trim();
  const history = knock.history || [];
  document.getElementById('bs-content').innerHTML = `
    <div class="bs-header">
      <div class="bs-header-pin" id="bs-header-pin" style="background:${s.color}">${s.icon}</div>
      <div class="bs-header-info">
        <h2>${knock.rep || 'No Name'}</h2>
        <div class="bs-status-label" id="bs-header-status">${s.label}</div>
      </div>
      <button class="bs-see-activity" onclick="editKnock('${knock.id}')">Edit ✏️</button>
    </div>
    <div class="bs-address">${line1}${line2 ? '<br>'+line2 : ''}</div>
    ${knock.notes ? `<div class="bs-notes">📝 ${knock.notes}</div>` : ''}
    <div class="bs-meta">${new Date(knock.time).toLocaleString()}</div>
    <div class="bs-divider"></div>
    <div style="font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:1px;color:#888;margin-bottom:8px">Change Status</div>
    <div class="bs-status-row">${STATUSES.map(st => `
      <div class="bs-status-item" onclick="quickStatus('${knock.id}','${st.key}')">
        <button class="bs-status-btn" data-key="${st.key}" style="background:${st.color};transform:${knock.status===st.key?'scale(1.15)':'scale(1)'};box-shadow:${knock.status===st.key?'0 0 0 3px white':'none'}">${st.icon}</button>
        <span class="bs-status-abbr">${ABBREVS[st.key]||st.key}</span>
      </div>`).join('')}
    </div>
    ${history.length > 0 ? `
    <div class="bs-divider"></div>
    <div style="font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:1px;color:#888;margin-bottom:8px">Status History</div>
    <div style="display:flex;flex-direction:column;gap:6px">
      <div style="display:flex;align-items:center;gap:8px">
        <div style="width:28px;height:28px;border-radius:50%;background:${s.color};display:flex;align-items:center;justify-content:center;font-size:13px;color:white;font-weight:900;flex-shrink:0">${s.icon}</div>
        <div style="flex:1"><div style="font-size:13px;font-weight:700;color:#111">${s.label}</div><div style="font-size:11px;color:#888">Current status</div></div>
        <div style="font-size:11px;color:#888">${new Date(knock.time).toLocaleString([], { month:'short', day:'numeric', hour:'2-digit', minute:'2-digit' })}</div>
      </div>
      ${history.slice(0,5).map(h => {
        const hs = STATUSES.find(x => x.key === h.status) || STATUSES[0];
        return `<div style="display:flex;align-items:center;gap:8px;opacity:0.7">
          <div style="width:28px;height:28px;border-radius:50%;background:${hs.color};display:flex;align-items:center;justify-content:center;font-size:13px;color:white;font-weight:900;flex-shrink:0">${hs.icon}</div>
          <div style="flex:1"><div style="font-size:13px;font-weight:600;color:#111">${hs.label}</div></div>
          <div style="font-size:11px;color:#888">${new Date(h.time).toLocaleString([], { month:'short', day:'numeric', hour:'2-digit', minute:'2-digit' })}</div>
        </div>`;
      }).join('')}
    </div>` : ''}
    <div class="bs-actions">
      <button class="bs-edit-btn" onclick="editKnock('${knock.id}')">✏️ Edit Details</button>
      <button class="bs-delete-btn" onclick="deleteKnock('${knock.id}');closeBottomSheet()">🗑 Delete</button>
    </div>`;
  document.getElementById('bottom-sheet').classList.add('open');
  document.getElementById('bottom-sheet-overlay').classList.add('open');
}
function closeBottomSheet() {
  document.getElementById('bottom-sheet').classList.remove('open');
  document.getElementById('bottom-sheet-overlay').classList.remove('open');
}

// ── DETAIL PAGE ──
window.editKnock = function(id) {
  closeBottomSheet();
  currentDetailId = id; currentDetailTab = 'info';
  renderDetailPage(id, 'info');
  document.getElementById('detail-page').classList.add('open');
};
function closeDetailPage() { document.getElementById('detail-page').classList.remove('open'); currentDetailId = null; }
window.deleteCurrentKnock = function() { if (!currentDetailId) return; if (!confirm('Delete this knock?')) return; deleteKnock(currentDetailId); closeDetailPage(); };
window.switchTab = function(tab) {
  currentDetailTab = tab;
  document.querySelectorAll('.detail-tab').forEach(t => t.classList.toggle('active', t.textContent.toLowerCase() === tab));
  renderDetailPage(currentDetailId, tab);
};

function renderDetailPage(id, tab) {
  const knock = knocks.find(k => k.id === id);
  if (!knock) return;
  const s = STATUSES.find(x => x.key === knock.status) || STATUSES[0];
  const content = document.getElementById('detail-content');

  if (tab === 'info') {
    const ap = knock.address.split(',');
    const street = ap[0]?.trim()||'', city = ap[1]?.trim()||'', stateZip = ap.slice(2).join(',').trim();
    content.innerHTML = `<div style="background:#fff">
      <div style="display:flex;gap:14px;padding:14px 18px;border-bottom:1px solid #e5e5ea">
        <div style="width:48px;height:48px;border-radius:50%;background:#38bdf8;display:flex;align-items:center;justify-content:center;cursor:pointer" title="Call"><svg width="20" height="20" viewBox="0 0 24 24" fill="none"><path d="M6.6 10.8c1.4 2.8 3.8 5.1 6.6 6.6l2.2-2.2c.3-.3.7-.4 1-.2 1.1.4 2.3.6 3.6.6.6 0 1 .4 1 1V20c0 .6-.4 1-1 1-9.4 0-17-7.6-17-17 0-.6.4-1 1-1h3.5c.6 0 1 .4 1 1 0 1.3.2 2.5.6 3.6.1.3 0 .7-.2 1L6.6 10.8z" fill="white"/></svg></div>
        <div style="width:48px;height:48px;border-radius:50%;background:#38bdf8;display:flex;align-items:center;justify-content:center;cursor:pointer" title="Text"><svg width="20" height="20" viewBox="0 0 24 24" fill="none"><path d="M20 2H4c-1.1 0-2 .9-2 2v18l4-4h14c1.1 0 2-.9 2-2V4c0-1.1-.9-2-2-2z" fill="white"/></svg></div>
        <div style="width:48px;height:48px;border-radius:50%;background:#38bdf8;display:flex;align-items:center;justify-content:center;cursor:pointer" title="Email"><svg width="20" height="20" viewBox="0 0 24 24" fill="none"><path d="M20 4H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 4l-8 5-8-5V6l8 5 8-5v2z" fill="white"/></svg></div>
        <div style="width:48px;height:48px;border-radius:50%;background:#38bdf8;display:flex;align-items:center;justify-content:center;cursor:pointer" title="Directions" onclick="window.open('https://maps.google.com?q=${encodeURIComponent(knock.address)}','_blank')"><svg width="20" height="20" viewBox="0 0 24 24" fill="none"><path d="M12 2L4.5 20.3l.7.7L12 18l6.8 3 .7-.7L12 2z" fill="white"/></svg></div>
      </div>
      <div style="background:#f2f2f7;padding:7px 18px 4px;font-size:11px;font-weight:700;color:#888;letter-spacing:1px;text-transform:uppercase">Contact Info</div>
      <div style="background:#fff;padding:0 18px">
        <input id="inf-business" value="${knock.business||''}" placeholder="Business Name" style="width:100%;border:none;border-bottom:1px solid #e5e5ea;padding:12px 0;font-size:15px;color:#111;outline:none;font-family:'DM Sans',sans-serif;background:transparent"/>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:0 14px">
          <input id="inf-fname" value="${knock.firstName||''}" placeholder="First Name" style="border:none;border-bottom:1px solid #e5e5ea;padding:12px 0;font-size:15px;color:#111;outline:none;font-family:'DM Sans',sans-serif;background:transparent;width:100%"/>
          <input id="inf-lname" value="${knock.lastName||''}" placeholder="Last Name" style="border:none;border-bottom:1px solid #e5e5ea;padding:12px 0;font-size:15px;color:#111;outline:none;font-family:'DM Sans',sans-serif;background:transparent;width:100%"/>
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:0 14px">
          <input id="inf-phone" value="${knock.phone||''}" placeholder="Phone" type="tel" style="border:none;border-bottom:1px solid #e5e5ea;padding:12px 0;font-size:15px;color:#111;outline:none;font-family:'DM Sans',sans-serif;background:transparent;width:100%"/>
          <input id="inf-phone2" value="${knock.phone2||''}" placeholder="Alt Phone" type="tel" style="border:none;border-bottom:1px solid #e5e5ea;padding:12px 0;font-size:15px;color:#111;outline:none;font-family:'DM Sans',sans-serif;background:transparent;width:100%"/>
        </div>
        <input id="inf-email" value="${knock.email||''}" placeholder="Email" type="email" style="width:100%;border:none;border-bottom:1px solid #e5e5ea;padding:12px 0;font-size:15px;color:#111;outline:none;font-family:'DM Sans',sans-serif;background:transparent"/>
        <div style="display:flex;align-items:center;gap:8px;border-bottom:1px solid #e5e5ea">
          <input id="inf-street" value="${street}" placeholder="Street Address" style="flex:1;border:none;padding:12px 0;font-size:15px;color:#111;outline:none;font-family:'DM Sans',sans-serif;background:transparent"/>
          <input id="inf-apt" value="${knock.apt||''}" placeholder="Apt" style="width:70px;border:none;padding:12px 0;font-size:15px;color:#111;outline:none;font-family:'DM Sans',sans-serif;background:transparent;text-align:right"/>
        </div>
        <input id="inf-city" value="${city}" placeholder="City" style="width:100%;border:none;border-bottom:1px solid #e5e5ea;padding:12px 0;font-size:15px;font-weight:700;color:#111;outline:none;font-family:'DM Sans',sans-serif;background:transparent"/>
      </div>
      <div style="background:#f2f2f7;padding:7px 18px 4px;font-size:11px;font-weight:700;color:#888;letter-spacing:1px;text-transform:uppercase;margin-top:7px">Status</div>
      <div style="background:#fff;padding:12px 18px">
        <div style="display:flex;gap:10px;overflow-x:auto;padding-bottom:6px;scrollbar-width:none">
          ${STATUSES.map(st => `<div style="display:flex;flex-direction:column;align-items:center;gap:4px;flex-shrink:0;cursor:pointer" onclick="quickStatusDetail('${id}','${st.key}')">
            <div style="width:50px;height:50px;border-radius:50%;background:${st.color};display:flex;align-items:center;justify-content:center;font-size:21px;color:white;font-weight:900;${knock.status===st.key?'box-shadow:0 0 0 3px white,0 0 0 5px '+st.color:''}">${st.icon}</div>
            <span style="font-size:10px;font-weight:700;color:#555;font-family:'DM Sans',sans-serif">${ABBREVS[st.key]||st.key}</span>
          </div>`).join('')}
        </div>
      </div>
      <div style="background:#f2f2f7;padding:7px 18px 4px;font-size:11px;font-weight:700;color:#888;letter-spacing:1px;text-transform:uppercase;margin-top:7px">Notes</div>
      <div style="background:#fff;padding:10px 18px">
        <textarea id="detail-notes-input" placeholder="Add notes…" style="width:100%;background:#f2f2f7;border:none;border-radius:10px;padding:11px 13px;font-family:'DM Sans',sans-serif;font-size:14px;color:#111;outline:none;resize:none;min-height:80px">${knock.notes||''}</textarea>
      </div>
      <div style="background:#f2f2f7;padding:7px 18px 4px;font-size:11px;font-weight:700;color:#888;letter-spacing:1px;text-transform:uppercase;margin-top:7px">Appointment</div>
      <div style="background:#fff;padding:14px 18px;border-bottom:1px solid #f2f2f7">
        ${knock.appointment
          ? `<div style="font-size:15px;font-weight:600;color:#111">📅 ${new Date(knock.appointment).toLocaleString()}</div><div style="margin-top:6px"><span style="font-size:13px;color:#e05c5c;cursor:pointer;font-weight:600" onclick="clearAppointment('${id}')">Clear</span></div>`
          : `<div style="text-align:center"><span style="font-size:15px;font-weight:700;color:#38bdf8;cursor:pointer" onclick="showAppointmentPicker('${id}')">Set Appointment</span></div>`}
        <input type="datetime-local" id="appt-picker" style="display:none;margin-top:10px;width:100%;padding:8px;border:1px solid #e5e5ea;border-radius:8px;font-family:'DM Sans',sans-serif;font-size:14px" onchange="saveAppointment('${id}')"/>
      </div>
      <div style="background:#f2f2f7;padding:7px 18px 4px;font-size:11px;font-weight:700;color:#888;letter-spacing:1px;text-transform:uppercase;margin-top:7px">Custom Fields</div>
      <div style="background:#fff;padding:4px 18px 8px">
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:0 14px">
          <div><div style="font-size:13px;font-weight:700;color:#38bdf8;padding-top:10px">Developer</div><input id="inf-developer" value="${knock.developer||''}" style="width:100%;border:none;border-bottom:1px solid #e5e5ea;padding:8px 0;font-size:14px;color:#111;outline:none;font-family:'DM Sans',sans-serif;background:transparent"/></div>
          <div><div style="font-size:13px;color:#aaa;padding-top:10px">Annual KwH</div><input id="inf-kwh" value="${knock.annualKwh||''}" style="width:100%;border:none;border-bottom:1px solid #e5e5ea;padding:8px 0;font-size:14px;color:#111;outline:none;font-family:'DM Sans',sans-serif;background:transparent"/></div>
        </div>
        <div><div style="font-size:13px;font-weight:700;color:#38bdf8;padding-top:12px">Utility</div><input id="inf-utility" value="${knock.utility||''}" style="width:100%;border:none;border-bottom:1px solid #e5e5ea;padding:8px 0;font-size:14px;color:#111;outline:none;font-family:'DM Sans',sans-serif;background:transparent"/></div>
      </div>
      <button class="detail-save-btn" onclick="saveDetailInfo('${id}')">Save Changes</button>
      <div style="height:28px"></div>
    </div>`;
  } else {
    const history = knock.history || [];
    const allActivity = [
      { status:knock.status, action:'Status updated', time:knock.time },
      ...history.map(h => ({ status:h.status, action:'Status updated', time:h.time })),
      { status:knock.status, action:'Lead created', time:knock.created||knock.time, isCreated:true },
    ];
    content.innerHTML = `
      <div class="activity-meta-row"><span>Last updated</span><span>${new Date(knock.time).toLocaleString([], { month:'numeric', day:'numeric', year:'2-digit', hour:'2-digit', minute:'2-digit' })}</span></div>
      ${allActivity.map(a => {
        const as = STATUSES.find(x => x.key === a.status) || STATUSES[0];
        const pinColor = a.isCreated ? '#22c55e' : '#e05c5c';
        return `<div class="activity-item">
          <div class="activity-pin-circle" style="background:${as.color}">${as.icon}</div>
          <div class="activity-info">
            <h3>${as.label} (${ABBREVS[a.status]||a.status})</h3>
            <p>${a.action} · Green Nova</p>
            <p>${new Date(a.time).toLocaleString([], { month:'numeric', day:'numeric', year:'2-digit', hour:'2-digit', minute:'2-digit' })}</p>
          </div>
          <div class="activity-map-pin">
            <svg width="22" height="30" viewBox="0 0 24 32" fill="none">
              <path d="M12 2C7.582 2 4 5.582 4 10c0 6 8 16 8 16s8-10 8-16C20 5.582 16.418 2 12 2z" fill="${pinColor}" opacity="0.2"/>
              <path d="M12 2C7.582 2 4 5.582 4 10c0 6 8 16 8 16s8-10 8-16C20 5.582 16.418 2 12 2z" stroke="${pinColor}" stroke-width="1.5" fill="none"/>
              ${a.isCreated ? `<path d="M9 10l2 2 4-4" stroke="${pinColor}" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>` : `<path d="M9.5 7.5l5 5M14.5 7.5l-5 5" stroke="${pinColor}" stroke-width="1.5" stroke-linecap="round"/>`}
            </svg>
          </div>
        </div>`;
      }).join('')}`;
  }
}

window.saveDetailInfo = function(id) {
  const idx = knocks.findIndex(k => k.id === id);
  if (idx !== -1) {
    const g = elId => document.getElementById(elId)?.value?.trim() || '';
    Object.assign(knocks[idx], { business:g('inf-business'), firstName:g('inf-fname'), lastName:g('inf-lname'), phone:g('inf-phone'), phone2:g('inf-phone2'), email:g('inf-email'), apt:g('inf-apt'), developer:g('inf-developer'), annualKwh:g('inf-kwh'), utility:g('inf-utility'), notes:document.getElementById('detail-notes-input')?.value?.trim() || knocks[idx].notes });
    const fullName = [knocks[idx].firstName, knocks[idx].lastName].filter(Boolean).join(' ');
    if (fullName) knocks[idx].rep = fullName;
    updateKnock(knocks[idx]); renderList(); updateStats();
    const btn = document.querySelector('.detail-save-btn');
    if (btn) { btn.textContent = '✓ Saved!'; btn.style.background = '#22c55e'; setTimeout(() => { btn.textContent = 'Save Changes'; btn.style.background = '#111'; }, 1500); }
  }
};

window.quickStatusDetail = function(id, statusKey) {
  const idx = knocks.findIndex(k => k.id === id);
  if (idx !== -1) {
    if (!knocks[idx].history) knocks[idx].history = [];
    knocks[idx].history.unshift({ status:knocks[idx].status, time:new Date().toISOString() });
    knocks[idx].status = statusKey;
    if (markers[id]) markers[id].setIcon(makeMarkerIcon(statusKey));
    updateKnock(knocks[idx]); renderList(); updateStats();
    renderDetailPage(id, currentDetailTab);
  }
};
window.showAppointmentPicker = function(id) { const p = document.getElementById('appt-picker'); if (p) { p.style.display = 'block'; p.focus(); } };
window.saveAppointment = function(id) { const p = document.getElementById('appt-picker'); if (!p) return; const idx = knocks.findIndex(k => k.id === id); if (idx !== -1) { knocks[idx].appointment = p.value; updateKnock(knocks[idx]); renderDetailPage(id, 'info'); } };
window.clearAppointment = function(id) { const idx = knocks.findIndex(k => k.id === id); if (idx !== -1) { delete knocks[idx].appointment; updateKnock(knocks[idx]); renderDetailPage(id, 'info'); } };

window.toggleSection = function(id) {
  const body = document.getElementById(id);
  const arrow = document.getElementById('arrow-' + id);
  if (!body) return;
  const collapsed = body.classList.contains('collapsed');
  body.classList.toggle('collapsed', !collapsed);
  if (arrow) arrow.classList.toggle('rotated', !collapsed);
};

// ── DASHBOARD ──
function filterByPeriod(data, period) {
  if (period === 'all') return data;
  const now = new Date(), start = new Date();
  if (period === 'today') start.setHours(0,0,0,0);
  else if (period === 'week') { start.setDate(now.getDate() - now.getDay()); start.setHours(0,0,0,0); }
  else if (period === 'month') { start.setDate(1); start.setHours(0,0,0,0); }
  else if (period === 'year') { start.setMonth(0,1); start.setHours(0,0,0,0); }
  return data.filter(k => new Date(k.time) >= start);
}

window.setTimePeriod = function(period) {
  currentTimePeriod = period;
  document.querySelectorAll('.time-tab').forEach(t => t.classList.toggle('active-time', t.dataset.period === period));
  renderDashboard();
};

function showDashboard() { renderDashboard(); document.getElementById('dashboard').style.display = 'flex'; }
function hideDashboard() { document.getElementById('dashboard').style.display = 'none'; }

document.getElementById('open-map-btn').addEventListener('click', () => {
  const rep = document.getElementById('dashboard-rep-name').value.trim();
  if (rep) window._defaultRep = rep;
  hideDashboard();
});

function renderDashboard() {
  const rep = document.getElementById('dashboard-rep-name').value.trim().toLowerCase();
  const period = filterByPeriod(knocks, currentTimePeriod);
  const mine = rep ? period.filter(k => k.rep && k.rep.toLowerCase().includes(rep)) : [];
  renderStatsGrid('my-stats-grid', mine);
  renderStatsGrid('company-stats-grid', period);
  renderDispositions('my-dispositions', mine);
  renderDispositions('company-dispositions', period);
}

function calcMetrics(data) {
  const totalDoors = data.length;
  const interactions = data.filter(k => !NON_INTERACTIONS.has(k.status)).length;
  const enrolled = data.filter(k => k.status === 'enrolled').length;
  const qualityLeads = data.filter(k => k.status === 'quality-lead').length;
  const leads = data.filter(k => k.status === 'lead').length;
  const appointments = data.filter(k => k.appointment).length;
  const notHome = data.filter(k => k.status === 'not-home').length;
  const notInterested = data.filter(k => k.status === 'not-interested').length;
  const missedLeads = data.filter(k => k.status === 'missed-lead').length;
  const closeRate = interactions > 0 ? ((enrolled/interactions)*100).toFixed(1) : '0.0';
  const apptRate = interactions > 0 ? ((appointments/interactions)*100).toFixed(1) : '0.0';
  const leadConv = interactions > 0 ? (((enrolled+qualityLeads)/interactions)*100).toFixed(1) : '0.0';
  return { totalDoors, interactions, enrolled, qualityLeads, leads, appointments, notHome, notInterested, missedLeads, closeRate, apptRate, leadConv };
}

function renderStatsGrid(containerId, data) {
  const m = calcMetrics(data);
  const cards = [
    { label:'Total Doors', value:m.totalDoors, color:'#7a8099', icon:'🚪', sub:'all pins dropped' },
    { label:'Interactions', value:m.interactions, color:'#a78bfa', icon:'🤝', sub:'qualifying contacts' },
    { label:'Enrolled', value:m.enrolled, color:'#22c55e', icon:'☀', sub:'signed up' },
    { label:'Quality Leads', value:m.qualityLeads, color:'#38bdf8', icon:'↻', sub:'strong interest' },
    { label:'Leads', value:m.leads, color:'#f97316', icon:'📍', sub:'follow up needed' },
    { label:'Appointments', value:m.appointments, color:'#bf8eff', icon:'📅', sub:'scheduled' },
    { label:'Close %', value:m.closeRate+'%', color:'#22c55e', icon:'📈', sub:'enrolled / interactions' },
    { label:'Appt Rate', value:m.apptRate+'%', color:'#bf8eff', icon:'🎯', sub:'appts / interactions' },
    { label:'Lead Conv %', value:m.leadConv+'%', color:'#38bdf8', icon:'⚡', sub:'enrolled+QL / interactions' },
    { label:'Not Home', value:m.notHome, color:'#ef4444', icon:'⌂', sub:'no answer' },
    { label:'Not Interested', value:m.notInterested, color:'#94a3b8', icon:'✕', sub:'declined' },
    { label:'Missed Leads', value:m.missedLeads, color:'#991b1b', icon:'📅', sub:'cancelled' },
  ];
  document.getElementById(containerId).innerHTML = `
    <div style="grid-column:1/-1;font-size:11px;color:var(--muted);font-weight:600;padding:2px 0 6px">⚠️ <em>Not Home, Not Qualified, Competitor & Spanish excluded from interaction metrics</em></div>
    ${cards.map(c => `<div class="stat-card" style="border-top-color:${c.color}">
      <div class="stat-card-label"><div class="stat-card-icon" style="background:${c.color}22;color:${c.color}">${c.icon}</div>${c.label}</div>
      <div class="stat-card-value" style="color:${c.color}">${c.value}</div>
      <div class="stat-card-sub">${c.sub}</div>
    </div>`).join('')}`;
}

function renderDispositions(containerId, data) {
  const interactions = data.filter(k => !NON_INTERACTIONS.has(k.status)).length || 1;
  const total = data.length || 1;
  const rows = STATUSES.map(s => {
    const count = data.filter(k => k.status === s.key).length;
    const isNI = NON_INTERACTIONS.has(s.key);
    return { s, count, pct:Math.round((count / (isNI ? total : interactions)) * 100), isNI };
  }).sort((a,b) => b.count - a.count);
  document.getElementById(containerId).innerHTML = rows.map(({ s, count, pct, isNI }) => `
    <div class="disposition-row" style="${isNI ? 'opacity:0.6' : ''}">
      <div class="disp-pin" style="background:${s.color}">${s.icon}</div>
      <div class="disp-label">${s.label}${isNI ? '<span style="font-size:9px;color:var(--muted);margin-left:4px;background:var(--border);padding:1px 5px;border-radius:4px">excluded</span>' : ''}</div>
      <div class="disp-bar-wrap"><div class="disp-bar" style="width:${pct}%;background:${s.color}"></div></div>
      <div class="disp-count" style="color:${s.color}">${count}</div>
    </div>`).join('');
}

document.getElementById('dashboard-rep-name').addEventListener('input', () => {
  window._defaultRep = document.getElementById('dashboard-rep-name').value.trim();
  renderDashboard();
});

// ── MOBILE NAV ──
window.mobileNav = function(tab) {
  document.querySelectorAll('.mobile-nav-btn').forEach((b,i) => b.classList.toggle('active-nav', ['map','legend','knocks','stats'][i] === tab));
  if (tab === 'legend') openMobileLegend();
  else if (tab === 'knocks') openMobileKnocks();
  else if (tab === 'stats') showDashboard();
};

function openMobileLegend() {
  const grid = document.getElementById('mobile-legend-grid');
  grid.innerHTML = '';
  STATUSES.forEach(s => {
    const el = document.createElement('div');
    el.style.cssText = 'display:flex;align-items:center;gap:10px;padding:9px 10px;border-radius:8px;background:var(--surface2);margin-bottom:6px;cursor:pointer;border:1.5px solid transparent';
    el.innerHTML = `<div style="width:28px;height:28px;border-radius:50%;background:${s.color};display:flex;align-items:center;justify-content:center;font-size:14px;color:#fff;font-weight:900;flex-shrink:0">${s.icon}</div><div><div style="font-size:13px;font-weight:700;color:var(--text)">${s.label}</div><div style="font-size:11px;color:var(--muted)">${s.desc}</div></div>`;
    el.addEventListener('click', () => { toggleFilter(s.key, el); closeMobileDrawer('legend'); });
    grid.appendChild(el);
  });
  document.getElementById('mobile-legend').style.display = 'block';
}

function openMobileKnocks() {
  const list = document.getElementById('mobile-knocks-list');
  const filtered = activeFilter ? knocks.filter(k => k.status === activeFilter) : knocks;
  if (filtered.length === 0) { list.innerHTML = '<div style="text-align:center;padding:28px;color:var(--muted)">🚪 No knocks yet</div>'; }
  else {
    list.innerHTML = filtered.map(k => {
      const s = STATUSES.find(x => x.key === k.status);
      const color = s ? s.color : '#888';
      return `<div style="background:var(--surface2);border:1px solid var(--border);border-left:3px solid ${color};border-radius:8px;padding:10px 12px;margin-bottom:7px;cursor:pointer" onclick="flyToKnock('${k.id}');closeMobileDrawer('knocks')">
        <div style="display:flex;justify-content:space-between;align-items:flex-start;gap:8px">
          <div style="font-size:13px;font-weight:600;flex:1">${k.address.split(',').slice(0,2).join(',')}</div>
          <div style="font-size:10px;font-weight:700;padding:2px 6px;border-radius:10px;background:${color}22;color:${color};white-space:nowrap">${s?s.icon+' '+s.label:k.status}</div>
        </div>
        ${k.rep ? `<div style="font-size:11px;color:var(--muted);margin-top:4px">👤 ${k.rep}</div>` : ''}
        <div style="font-size:10px;color:var(--muted);margin-top:3px">${new Date(k.time).toLocaleDateString([], { month:'short', day:'numeric' })} · ${new Date(k.time).toLocaleTimeString([], { hour:'2-digit', minute:'2-digit' })}</div>
      </div>`;
    }).join('');
  }
  document.getElementById('mobile-knocks').style.display = 'block';
}

window.closeMobileDrawer = function(type) {
  document.getElementById('mobile-' + type).style.display = 'none';
  document.querySelectorAll('.mobile-nav-btn').forEach((b,i) => b.classList.toggle('active-nav', i === 0));
};

// ── MY LOCATION ──
window.goToMyLocation = function() {
  if (!navigator.geolocation) { alert('Geolocation not supported on this device.'); return; }
  navigator.geolocation.getCurrentPosition(pos => {
    const loc = { lat: pos.coords.latitude, lng: pos.coords.longitude };
    map.panTo(loc);
    map.setZoom(17);
    // Show a blue dot for current location
    if (window._myLocationMarker) window._myLocationMarker.setMap(null);
    window._myLocationMarker = new google.maps.Marker({
      position: loc, map,
      icon: {
        path: google.maps.SymbolPath.CIRCLE,
        scale: 10,
        fillColor: '#4f8ef7',
        fillOpacity: 1,
        strokeColor: '#fff',
        strokeWeight: 3,
      },
      title: 'Your location',
      zIndex: 999,
    });
  }, () => alert('Could not get your location. Make sure location access is enabled.'));
};

// ── ROUTE TRACKER ──
let routeTracking = false;
let routePath = [];
let routePolyline = null;
let routeWatchId = null;

window.toggleRouteTracker = function() {
  routeTracking = !routeTracking;
  const btn = document.getElementById('route-btn');
  const badge = document.getElementById('route-badge');

  if (routeTracking) {
    routePath = [];
    btn.style.background = '#22c55e';
    badge.style.display = 'block';
    if (routePolyline) { routePolyline.setMap(null); routePolyline = null; }
    routePolyline = new google.maps.Polyline({
      path: routePath,
      geodesic: true,
      strokeColor: '#22c55e',
      strokeOpacity: 0.8,
      strokeWeight: 4,
      map,
    });
    routeWatchId = navigator.geolocation.watchPosition(pos => {
      const point = { lat: pos.coords.latitude, lng: pos.coords.longitude };
      routePath.push(point);
      routePolyline.setPath(routePath);
      // Update location dot
      if (window._myLocationMarker) window._myLocationMarker.setPosition(point);
    }, null, { enableHighAccuracy: true, maximumAge: 5000, timeout: 10000 });
  } else {
    btn.style.background = 'rgba(26,29,39,0.9)';
    badge.style.display = 'none';
    if (routeWatchId) navigator.geolocation.clearWatch(routeWatchId);
    // Keep the drawn path visible but stop tracking
  }
};

// ── HEATMAP ──
let heatmapLayer = null;
let heatmapActive = false;

window.toggleHeatmap = function() {
  heatmapActive = !heatmapActive;
  const btn = document.getElementById('heatmap-btn');

  if (heatmapActive) {
    btn.style.background = '#f97316';
    // Weight successful statuses higher
    const weightMap = { 'enrolled': 5, 'quality-lead': 4, 'lead': 3, 'missed-lead': 2, 'not-home': 1, 'not-interested': 1, 'not-qualified': 1, 'competitor': 1, 'jerk': 1, 'solar-roof': 1, 'spanish': 1 };
    const heatData = knocks.map(k => ({
      location: new google.maps.LatLng(k.lat, k.lng),
      weight: weightMap[k.status] || 1,
    }));
    if (heatmapLayer) heatmapLayer.setMap(null);
    heatmapLayer = new google.maps.visualization.HeatmapLayer({
      data: heatData,
      map,
      radius: 40,
      gradient: ['rgba(0,0,0,0)', '#38bdf8', '#22c55e', '#f97316', '#ef4444'],
    });
  } else {
    btn.style.background = 'rgba(26,29,39,0.9)';
    if (heatmapLayer) { heatmapLayer.setMap(null); heatmapLayer = null; }
  }
};

// ── ALREADY KNOCKED CHECK ──
function checkAlreadyKnocked(lat, lng) {
  const THRESHOLD = 0.0001; // ~10 meters
  return knocks.find(k => Math.abs(k.lat - lat) < THRESHOLD && Math.abs(k.lng - lng) < THRESHOLD);
}

function darkMapStyle() {
  return [
    { elementType:'geometry', stylers:[{ color:'#1a1d27' }] },
    { elementType:'labels.text.stroke', stylers:[{ color:'#1a1d27' }] },
    { elementType:'labels.text.fill', stylers:[{ color:'#7a8099' }] },
    { featureType:'administrative.locality', elementType:'labels.text.fill', stylers:[{ color:'#a0aabf' }] },
    { featureType:'poi', elementType:'labels.text.fill', stylers:[{ color:'#5a6080' }] },
    { featureType:'poi.park', elementType:'geometry', stylers:[{ color:'#151821' }] },
    { featureType:'road', elementType:'geometry', stylers:[{ color:'#2e3347' }] },
    { featureType:'road', elementType:'geometry.stroke', stylers:[{ color:'#22263a' }] },
    { featureType:'road.highway', elementType:'geometry', stylers:[{ color:'#3a3f5c' }] },
    { featureType:'road.highway', elementType:'geometry.stroke', stylers:[{ color:'#1a1d27' }] },
    { featureType:'transit', elementType:'geometry', stylers:[{ color:'#22263a' }] },
    { featureType:'water', elementType:'geometry', stylers:[{ color:'#0d1017' }] },
    { featureType:'water', elementType:'labels.text.fill', stylers:[{ color:'#3d4663' }] },
  ];
}
</script>

<script async defer src="https://maps.googleapis.com/maps/api/js?key=AIzaSyAyS5-AK1G0rZNrn0vsdCn9_PkKzpxiV3A&libraries=places,visualization&callback=initMap"></script>
</body>
</html>
